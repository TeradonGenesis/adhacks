import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#B928CB",
    },

    border: {
      main: "#373737",
    },

    alert: {
      main: "#D10101",
    },
  },
});

export default theme;
