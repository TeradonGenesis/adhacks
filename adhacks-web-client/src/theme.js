import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#B928CB",
    },

    secondary: {
      main: "#0999E2",
    },

    border: {
      main: "#373737",
    },

    alert: {
      main: "#D10101",
    },
  },
});

export default theme;
